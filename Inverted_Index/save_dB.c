#include "invertedIndex.h"

int save_DB(main_node_t **head, char *fname)
{
    /* Open the file write mode */

    /* run a loop */

    /* find the non empty index */

    /* print the details inside the file in give format */

    /* #index_no;
       word;file_count;filename;word_occurance;
     */


    printf("Successfull : Database saved in file %s\n", fname);
    return SUCCESS;
}
